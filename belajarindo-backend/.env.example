# Example environment variables for BelajarIndo backend
# Copy this file to .env and fill values before running

# MySQL connection string
DATABASE_URL=mysql://USER:PASSWORD@HOST:PORT/DATABASE

# JWT secret for signing tokens (change in production)
JWT_SECRET=change_me_to_a_secure_value

# Server settings
NODE_ENV=development
PORT=3000
